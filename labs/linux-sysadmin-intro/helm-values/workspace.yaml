vdi:
  image: ghcr.io/skillcertify/vdi-terminal
  tag: latest
  imagePullPolicy: Always
  permissions:
    root: true
  init_script: |
    #!/bin/bash
    apt-get update -qq
    apt-get install -y -qq net-tools curl wget
